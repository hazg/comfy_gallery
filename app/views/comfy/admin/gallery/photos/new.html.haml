.page-header
	= link_to "Return to #{@gallery.title}", comfy_admin_gallery_photos_path(@site, @gallery)
	%h2 New Photo

= comfy_gallery_form_for @photo, :url => { :action => :create }, :html => { :multipart => true } do |form|
	= render form
